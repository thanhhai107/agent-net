# Configure IP addresses
ip address add 10.0.3.2/30 dev eth0
ip address add 10.0.4.1/30 dev eth1

# Set routes to enter the DC
ip route add 201.0.0.0/8 via 10.0.3.1 dev eth0
ip route add 101.0.0.0/8 via 10.0.3.1 dev eth0

# Start FRR
systemctl start frr